ignore *
unignore from: subject to cc date x-mailer x-url user-agent

source ~/.mutt.creds
source ~/.mutt.gpg

set pgp_use_gpg_agent = yes
set pgp_sign_as = 0x1F81112D62A9ADCE
set pgp_timeout = 3600
set crypt_autosign = no
set crypt_replyencrypt = yes
set crypt_verify_sig = yes

set editor = "vim"

set header_cache =  ~/.mutt/cache/headers
set message_cachedir = ~/.mutt/cache/bodies
set certificate_file = ~/.mutt/certificates

set mail_check = 120
set mail_check_stats = yes
set timeout = 300
set imap_keepalive = 300
set imap_passive
set imap_check_subscribed = yes
set ispell = "aspell --mode=email --add-email-quote=%,#,:,} --check"
set message_cache_clean = yes
set user_agent = no
set smart_wrap = yes

set move = no

set askcc

set sort = 'threads'
set sort_aux = 'last-date-received'

set mailcap_path="~/.mailcap"

set sidebar_visible = yes
set sidebar_width = 30
#set sidebar_format = "%B%* (%N)%S"
set sidebar_format = "%B%?F? [%F]?%* %?N?%N/?%S"

set pager_index_lines=10

set spoolfile = "="
set record="=INBOX.Sent Items"
set postponed="=INBOX.Drafts"
set trash = "=INBOX.Trash"

set query_command= "ruby25 ~/bin/card_query.rb '%s'"
bind editor        <Tab> complete-query

#auto_view text/html                                      # view html automatically
#alternative_order text/plain text/enriched text/html     # save html for last

macro index B '<enter-command>toggle sidebar_visible<enter><refresh>'
macro pager B '<enter-command>toggle sidebar_visible<enter><redraw-screen>'

bind index,pager \Ck sidebar-prev
bind index,pager \Cj sidebar-next
bind index,pager \Co sidebar-open

folder-hook . set from="aaron@bolddaemon.com"

#reply-hook '~t .@openbsd\.org' 'set from="abieber@openbsd.org"'
#send-hook '~t .*@openbsd\.org' 'set from="abieber@openbsd.org"'

#macro index t "<pipe-message>mutt2task<enter> <save-message>+TODO<enter>"

macro index \cb "|urlview\n"
macro pager \cb "|urlview\n"

#unmailboxes *

mailboxes = "=INBOX"
mailboxes = "=INBOX.OpenBSD.Hackers"
mailboxes = "=INBOX.OpenBSD.Ports"
mailboxes = "=INBOX.OpenBSD.Tech"
mailboxes = "=INBOX.OpenBSD.Misc"
mailboxes = "=INBOX.OpenBSD.Arm"
mailboxes = "=INBOX.OpenBSD.Bugs"
mailboxes = "=INBOX.OpenBSD.src-changes"
mailboxes = "=INBOX.OpenBSD.ports-changes"
mailboxes = "=INBOX.Root Mail"
mailboxes = "=INBOX.Speed Tests"
mailboxes = "=INBOX.OSS-Sec"

#subscribe "hackers@openbsd\\.org"
#subscribe "bugs@openbsd\\.org"
#subscribe "tech@openbsd\\.org"
#subscribe "ports@openbsd\\.org"
#subscribe "misc@openbsd\\.org"
#subscribe "src-changes@openbsd\\.org"

source ~/.mutt.theme

